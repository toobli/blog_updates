{
  "name": "shieldtx-site",
  "version": "2.0.0",
  "private": true,
  "description": "ShieldTX marketing site. Static HTML/CSS/JS with Vercel serverless functions in /api.",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "dev": "npx serve -l 3000 .",
    "deploy": "vercel deploy --prod",
    "sync": "python3 scripts/sync-partials.py"
  }
}
